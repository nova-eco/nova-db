#####################################################################
#                                                                   #
# Workflow: pr-validation.yml                                       #
#                                                                   #
# Purpose:  Validate pull requests by running code quality checks   #
#           before allowing merge to master                         #
#                                                                   #
# Author:   nova admin <admin@nova.eco>                             #
#                                                                   #
#####################################################################

name: Pull Request Validation

on:
  pull_request:
    branches:
      - master
    types:
      - opened
      - synchronize
      - reopened

jobs:
  validate:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: read

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Run format check
        run: npm run format:check

      - name: Run linting
        run: npm run lint

      - name: Run spell check
        run: npm run spell

      - name: Validation summary
        if: success()
        run: |
          echo "✅ All validation checks passed!"
          echo "✅ Code formatting is correct"
          echo "✅ Docker files are valid"
          echo "✅ No spelling errors found"
